import { showGridConfig } from '../ui/grid-config.js';

export function startPracticeSession(container: HTMLElement): void {
  showGridConfig(container);
}

export { startLevelSession } from './session.js';
